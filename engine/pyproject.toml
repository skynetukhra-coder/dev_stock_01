[project]
name = "prophecy-engine"
version = "0.1.0"
description = "Prophecy strategy and paper-trading engine"
requires-python = ">=3.9"

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I"]
